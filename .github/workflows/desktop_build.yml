name: Desktop Build

on:
  workflow_dispatch:
  push:
    tags:
      - 'v*'

jobs:
  build:
    name: Build ${{ matrix.target }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-latest
            target: linux
            bundle_root: build/linux/x64/release/bundle
            artifact_path: build/linux/x64/release/bundle
          - os: windows-latest
            target: windows
            bundle_root: build/windows/x64/runner/Release
            artifact_path: build/windows/x64/runner/Release
          - os: macos-latest
            target: macos
            bundle_root: build/macos/Build/Products/Release/app1.app/Contents/Resources
            artifact_path: build/macos/Build/Products/Release/app1.app

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: stable

      - name: Install Linux dependencies
        if: matrix.target == 'linux'
        run: |
          sudo apt-get update
          sudo apt-get install -y ninja-build libgtk-3-dev

      - name: Enable desktop target
        run: flutter config --enable-${{ matrix.target }}-desktop

      - name: Install dependencies
        run: flutter pub get

      - name: Build
        run: flutter build ${{ matrix.target }} --release

      - name: Bundle adb and scrcpy
        shell: bash
        run: |
          chmod +x scripts/bundle_tools.sh
          ./scripts/bundle_tools.sh "${{ matrix.target }}" "${{ matrix.bundle_root }}"

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: app1-${{ matrix.target }}
          path: ${{ matrix.artifact_path }}
          if-no-files-found: error
